{
  "name": "intuilab",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "@base-ui/react": "^1.4.0",
    "tailwindcss-animate": "^1.0.7"
  }
}
